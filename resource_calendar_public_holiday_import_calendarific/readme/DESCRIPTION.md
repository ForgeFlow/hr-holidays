Provider for *Resource Calendar Public Holiday Import* that fetches public
holidays from the [Calendarific](https://calendarific.com) API (national and
local holidays).

It requires an API key. Set it in the system parameter `calendarific.api_key`
(*Settings > Technical > System Parameters*).
